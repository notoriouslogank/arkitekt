

2. DETERMINE PACKAGE MANAGER
	a. If apt:
		i.	sudo apt update && sudo apt full-upgrade -y	
	b. If pacman:
		i.	sudo pacman -Syu
	c. If else:
		i. 	[RAISE EXCEPTION]
1. DETERMINE IF GUI INSTALL NEEDED
	a. If yes:
		
	b. If no:
3. BACKUP CONFIGS
	a. sudo mv /etc/ssh_config /etc/ssh_config.bak
	b. sudo mv /etc/sshd_config /etc/sshd_config.bak
	c. sudo mv 
4. REPLACE CONFIGS


